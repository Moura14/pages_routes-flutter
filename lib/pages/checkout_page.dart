import 'package:flutter/material.dart';

class CkeckoutPage extends StatelessWidget {
  const CkeckoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Checkout'),
      ),
    );
  }
}
